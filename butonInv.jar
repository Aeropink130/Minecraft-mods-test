package inventory;

import net.minecraft.client.gui.screen.ingame.HandledScreen;
import net.minecraft.client.gui.widget.AbstractButtonWidget;
import net.minecraft.text.LiteralText;
import net.minecraft.text.Text;

public class OrdenarItemsButton extends AbstractButtonWidget {

    public OrdenarItemsButton(int x, int y) {
        super(x, y, 100, 20, new LiteralText("Ordenar"));
    }

    @Override
    public void onPress() {
        // Lógica para ordenar los ítems en el inventario
        System.out.println("¡Ordenando los ítems en el inventario!");
    }

    @Override
    public void renderButton(int mouseX, int mouseY, float partialTicks) {
        // Apariencia del botón
        super.renderButton(mouseX, mouseY, partialTicks);
    }
}